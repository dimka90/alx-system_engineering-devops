# Set default policies to deny incoming and allow outgoing
uwf default deny incoming
uwf default allow outgoing
# Allow SSH (Port 22)
uwf allow 22/tcp
# Allow HTTPS (Port 443)
ufw allow 443/tcp
# Allow HTTP (Port 80)
ufw allow 80/tcp
# Enable UFW
ufw enable
